apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment
  namespace: microservices
  labels:
    app: microservices
    service: payment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: microservices
      service: payment
  template:
    metadata:
      labels:
        app: microservices
        service: payment
    spec:
      containers:
      - name: payment
        image: docker.io/DOCKERHUB_USERNAME/payment:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
          name: http
        env:
        - name: SERVICE_NAME
          value: "payment"
        - name: RABBITMQ_HOST
          valueFrom:
            configMapKeyRef:
              name: microservices-config
              key: RABBITMQ_HOST
        - name: RABBITMQ_USER
          valueFrom:
            configMapKeyRef:
              name: microservices-config
              key: RABBITMQ_USER
        - name: RABBITMQ_PASS
          valueFrom:
            secretKeyRef:
              name: microservices-secrets
              key: RABBITMQ_PASS
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /payment
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /payment
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: payment
  namespace: microservices
  labels:
    app: microservices
    service: payment
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: microservices
    service: payment
